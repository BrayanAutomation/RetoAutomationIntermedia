serenity {
  project.name = "Reto Automation - API Tests"
  take.screenshots = UNDEFINED
  report.encoding = utf8
  logging = VERBOSE
}

environments {
  default = "local"
  local {
    api.base.url = "http://localhost:8080"
  }
  docker {
    api.base.url = "http://api:8080"
  }
}

feature {
  file.encoding = utf8
}
